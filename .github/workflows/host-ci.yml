name: Host CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  format:
    name: Formatting
    runs-on: ubuntu-24.04

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install --yes clang-format

      - name: Verify C++ formatting
        run: |
          find apps -type f \( -name '*.cpp' -o -name '*.hpp' \) -print0 |
            xargs -0 clang-format --dry-run --Werror

  build-and-test:
    name: Build and unit test
    runs-on: ubuntu-24.04

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes clang clang-tidy ninja-build

      - name: Configure Debug Application
        run: cmake --preset debug-app

      - name: Build Debug Application
        run: cmake --build build/debug-app --parallel

      - name: Configure Debug Unit Tests
        run: cmake --preset debug-unit-tests

      - name: Build and run unit tests
        run: cmake --build build/debug-unit-tests --parallel